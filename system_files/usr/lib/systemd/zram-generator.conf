# zram swap configuration
# Replaces disk swap with compressed RAM-based swap
# swappiness=180 is set in /usr/lib/sysctl.d/99-swappiness.conf

[zram0]
zram-size = min(ram, 8192)
compression-algorithm = zstd
swap-priority = 100
